name: Update AI News

permissions:
  contents: write

on:
  schedule:
    # UTC 기준 매 4시간마다 (00,04,08,12,16,20 UTC)
    - cron: '0 */4 * * *'
  workflow_dispatch:

jobs:
  crawl-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0           # 전체 히스토리 가져오기 (commit 비교용)
          persist-credentials: true  # GITHUB_TOKEN을 git push에 자동 사용

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install feedparser openai

      - name: Ensure news directory
        run: mkdir -p News

      - name: Fetch AI News
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: |
          # 한국시간 기준 파일명 생성
          DATE=$(date -u -d '+9 hours' +'%Y-%m-%d-%H%M')
          python scripts/fetch_ai_news.py \
            --output News/${DATE}.md \
            --max-items 10

      - name: Commit & Push if changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add News/*.md
          if ! git diff --cached --quiet; then
            git commit -m "chore: update AI News summary for ${DATE}"
            git push
          else
            echo "No changes to commit, skipping."
          fi
